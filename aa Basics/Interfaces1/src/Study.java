//INTERFACE represents some behavior common for several classes
public interface Study {
    void readbook();     //Interface methods cannot have body (cause of diff. implementation for diff. classes)
    void dolab();        //all M. are public
}
