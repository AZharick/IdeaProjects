//INTERFACE represents some behavior common for several classes
public interface Relax {
    void watchTV();   //Interface methods cannot have body (cause of diff. implementation for diff. classes)
    void playsega();  //all M. are public
}
